# Secrets and local config
.env
.env.*
!.env.example
!configs/env.example
*.pem
*.key

# OS and editors
.DS_Store
.idea/
.vscode/
*.swp
*.swo

# Logs and runtime
logs/
*.log
tmp/
temp/

# Dashboard persisted state (local only)
data/transactions.json
data/portfolio.json
data/exchange-keys.json
data/trade-journal.json
data/positions.json
wallet_data.db

# Dependencies (when agents add Node/Python/etc.)
node_modules/
__pycache__/
.venv/
venv/
dist/
build/
*.egg-info/

# Downloaded / built relayer-cli (see scripts/install-relayer-cli.mjs)
tools/relayer-cli

# Optional: twilight-bot cloned from dashboard (Agentic tab) — use submodule instead if you want it tracked
external/twilight-bot/

# Tooling
coverage/
.coverage
htmlcov/
